# Fix config lookup

This public development task measures small-repository diagnosis, exact code editing, and regression preservation. Its verifier combines the fixture's unit tests with independent precedence cases.
